<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/contact_card"
    style="@style/MaterialCardStyle"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <!-- A ShapeableImageView that displays the contact picture -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/profile_picture"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_margin="5dp"

            app:shapeAppearanceOverlay="@style/RoundedImage"
            app:srcCompat="@drawable/baseline_account_circle_24"
            tools:srcCompat="@tools:sample/avatars" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical">
            <!-- A TextView that displays the contact name -->
            <TextView
                android:id="@+id/name_text_view"
                style="@style/Theme.MyDigitalExperience.PrimaryText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                tools:text="@tools:sample/full_names" />
            <!-- A TextView that displays the contact number -->
            <TextView
                android:id="@+id/age_text_view"
                style="@style/Theme.MyDigitalExperience.SecondaryText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                tools:text="age: 30" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>